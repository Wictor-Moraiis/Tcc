<UserControl x:Class="MarcenariaMorais.TextBoxPlaceholder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MarcenariaMorais"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid HorizontalAlignment="Center" 
          VerticalAlignment="Center">

        <TextBox x:Name="tbx_texto"
                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 VerticalAlignment="Top"
                 GotFocus="TextBox_GotFocus"
                 LostFocus="TextBox_LostFocus"
                 TextChanged="TextBox_TextChanged"/>

        <TextBlock x:Name="tb_placeholder" 
                   Text="{Binding TextPlaceholder, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Height="{Binding ActualHeight, ElementName=tbx_texto}"
                   VerticalAlignment="Top"
                   Margin="5 1 0 0"
                   Opacity="0.5"
                   IsHitTestVisible="False"/>
        
    </Grid>
</UserControl>
